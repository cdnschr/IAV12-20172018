#include <string.h>
#include <stdio.h>

void eingabe(char* display, int stringnr, char* text);
void *suche(const char* string1, const char* string2, const char* gesamt, int len);

int main(void){

  return 0;
}
